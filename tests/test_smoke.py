"""Placeholder smoke test.

The project has no real test coverage yet; this stub exists only so the
test suite collects at least one passing test (pytest exits 0 instead of
5 "no tests collected"). Replace with real tests as the app grows.
"""


def test_smoke():
    assert True
